name: opinion-balancer-minimal
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - transformers
    - torch --index-url https://download.pytorch.org/whl/cpu
    - pydantic
    - pyyaml
    - langgraph
